#include<iostream>
#include<iomanip>
using namespace std;

int main()
{
  int n, m, power;
  int i = 0;

  do{
    cout << "Please enter a number and the power";

    cin >> n >> m;
    cout << endl;
    if (0 < n && n > 10 && 1 > m && m > 10);
      check = true;{
      while(check==false);
    while(i < m){
        power = n*i;
        cout << power << endl;
      }
    } 
  }

}